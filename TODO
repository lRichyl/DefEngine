
- Add a Z coordinate to use a layer. So we don't have to draw things in the order we want them.
- Add the ability to set the type of filter we want for a texture when we create it. Right now it is always GL_NEAREST.
- Add AnimatedSprites.